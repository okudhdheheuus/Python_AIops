apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: itops
type: Opaque
stringData:
  # === 生产环境务必修改以下值 ===
  SECRET_KEY: "change-me-in-production-use-a-real-random-secret"
  # === LLM API Keys（至少配置一个） ===
  GLM_API_KEY: "your-glm-api-key-here"
  DEEPSEEK_API_KEY: ""
  OPENAI_API_KEY: ""

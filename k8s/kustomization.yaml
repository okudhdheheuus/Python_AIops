apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: itops

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - postgres-deployment.yaml
  - redis-deployment.yaml
  - backend-deployment.yaml
  - frontend-deployment.yaml
  - ingress.yaml

# 生产环境建议用 sealed-secrets 或 external-secrets 管理密钥
# secretGenerator:
#   - name: backend-secret
#     envs:
#       - .env.secret
